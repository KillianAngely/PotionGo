{
  "rules": {
    "drivers": {
      "$driverId": {
        "location": {
          ".read": "auth != null",
          ".write": "auth != null && auth.uid === $driverId"
        }
      }
    },
    ".read": false,
    ".write": false
  }
}
